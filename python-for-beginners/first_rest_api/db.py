from flask_sqlalchemy import SQLAlchemy

db = SQLAlchemy()

'''
Benefits of using SQLAlchemy: 
    ORM: object relational mapping(
        multi threading support,
        handles creating the tables and their columns 
        cleaner and shorter code
    )
'''